#!/bin/sh
if [ $# -eq 0 ]
then exec mplayer -shuffle ~/m/*/*
elif [ -d "$1" -o -f "$1" ]
then find "$@" -type f -exec mplayer -shuffle {} \+
else t=`mktemp`
if [ "$1" = p ]
then CM=mplayer
else CM="$1"
fi
shift
for a
do
if [ "${a:0:1}" = \| ]
then SH="$SH$a"
elif [ "$a" = r ]
then SH="$SH|sort -Rz"
elif [ "$a" = s ]
then SH="$SH|sort -z"
else FN="$FN "`printf %q "$a"`
fi
done
eval "find $FN -type f -print0$SH>\$t;exec xargs -0a \$t $CM"
rm $t
fi
